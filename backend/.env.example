APP_ENV=dev
APP_HOST=0.0.0.0
APP_PORT=8000
APP_LOG_LEVEL=info

# SQLite file. Relative to the directory you run uvicorn from (backend/).
APP_DB_PATH=data/superman.db

# JSON-encoded list parsed by pydantic-settings.
APP_CORS_ALLOW_ORIGINS=["http://localhost:5173"]
