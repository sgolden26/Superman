APP_ENV=dev
APP_HOST=0.0.0.0
APP_PORT=8000
APP_LOG_LEVEL=info

# Relative to backend/ when you run uvicorn from that directory. Repositories
# should read and write this file (created on first save).
APP_DATA_JSON_PATH=data/demo.json

GHOST_MURMUR_API_BASE=
SATELLITE_API_BASE=
DRONE_API_BASE=
