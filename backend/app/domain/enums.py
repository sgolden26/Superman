"""Enumerations shared across the domain.

Intentionally empty for now: the simplified surface (sensors and subjects with
a location) does not need shared enums. Reintroduce values here only when at
least two modules need them.
"""
from __future__ import annotations
