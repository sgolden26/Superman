# Frontend

React + TypeScript + Tailwind on Vite. Two views: `/c2` and `/field`.
Stubs only.

```bash
pnpm install
pnpm dev          # http://localhost:5173 (proxies /api -> backend on :8000)
pnpm typecheck    # tsc --noEmit
```

Conventions live in the root `AGENTS.md`. Read it first.
