/**
 * The default field view: nearest threat, classification, distance and
 * bearing, plus current sensor/network status. Designed to be readable
 * at a glance.
 */
export default function HeadsUpPage() {
  return <div className="p-4">Heads-Up</div>;
}
