# Superman: copy to `.env` at repo root or as `backend/.env`.
# Never commit real keys.

# Required for NL order suggestion and OpenAI-backed scenario tooling when those routes are used.
OPENAI_API_KEY=

# Optional: override the default OpenAI model for scenario designer flows (see package code for the default id).
SUPERMAN_AI_MODEL=
